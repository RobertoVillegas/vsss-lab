# M14 delivery evidence — 2026-07-28

## Repository gates

The final source state passed:

- `just doctor`;
- `just build`;
- `just test` (all Rust workspaces, 145 Python tests, and 4 viewer tests);
- `just lint` (rustfmt, Clippy with warnings denied, Ruff, mypy, and TypeScript);
- `just cuda-smoke` on an NVIDIA GeForce RTX 3070 with Torch 2.13.0+cu130;
- `openspec validate m14-evidence-driven-adaptive-training --strict`.

## End-to-end adaptive smoke

A one-iteration CUDA run used
`experiments/configs/m14-mappo-adaptive.toml`, two vector worlds, the committed
scenario suite, checkpointing, replay capture, replay analytics, TensorBoard,
and curriculum feedback. It completed 512 environment steps and produced a
registry, metrics, two checkpoint artifacts, canonical replay, observer
analysis, and TensorBoard event stream without an infrastructure failure.

Generated smoke/study files remain ignored; this note records the decision and
the durable commands without checking machine-specific run paths into source.

## Rollback

M14 features are bounded by explicit configuration choices. The prior M13
configuration remains available, Rapier remains authoritative, negative
ablation arms are not defaults, and promotion decisions never overwrite the
incumbent registry entry. M12 physical perception is retained but explicitly
deferred.
