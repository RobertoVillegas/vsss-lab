# M15 delivery evidence — 2026-07-28

## Implementation

- `949606f` — typed deterministic scenario families and causal predicates.
- `36d91ef` — vector rollout integration, mirrored control, curriculum, early
  termination, and terminal attribution.
- `71d3e3e` — evaluation, persistence, observability, viewer, probes,
  documentation, and machine-readable rejection.

All commits are signed. Final task/evidence closure is delivered by the
subsequent signed documentation commit.

## Gates

- `just doctor`: passed, zero failures.
- `just build`: passed, including Cargo, Python compilation, protocol checks,
  and Vite production build.
- `just test`: passed; 177 Python tests, 7 viewer tests, and all Rust workspace
  unit/integration/doc tests.
- `just lint`: passed; Cargo fmt/clippy, Ruff, mypy, and TypeScript.
- `just cuda-smoke`: passed on NVIDIA GeForce RTX 3070, CUDA 13.0,
  PyTorch 2.13.0+cu130.
- `openspec validate m15-semantic-skill-curriculum --strict`: passed.
- `docker ps` after gates: no VSSS Lab containers remain active.

## Training decision

The bounded candidate failed semantic learnability and frozen-M14 transfer.
`docs/evidence/m15-entry-decision.json` therefore records
`reject_large_run`, and deliberately contains no high-budget command. This is a
completed safety decision, not an unfinished promotion.
