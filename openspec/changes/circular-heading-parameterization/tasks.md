# Tasks

- [ ] Accept ADR 0014 before implementation begins.
- [ ] Add the circular heading contract: unbounded mean direction, per-state concentration.
- [ ] Sample, score, and measure entropy as circular quantities in rollout and update.
- [ ] Transport the sampled angle normalized by π inside the existing bounded contract.
- [ ] Assert rollout and update recover one log-probability, so the ratio starts at one.
- [ ] Move the teacher intensity target inside the reachable interval.
- [ ] Let requested authority inform strike intercept selection.
- [ ] Record the heading contract in the checkpoint and reject a mismatched load.
- [ ] Report angular concentration beside the heading-change statistic.
- [ ] Add the isotropy test, the log-probability equality test, the intercept
      reachability test, and the loader rejection test.
- [ ] Keep the previous parameterization selectable and green as the ablation baseline.
- [ ] Complete local gates and one end-to-end run reaching a paired evaluation.
- [ ] Record a fresh baseline; earlier M24.2 numbers are not comparable.
